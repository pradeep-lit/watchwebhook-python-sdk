[project]
name = "watchwebhook-python-sdk"
version = "0.1.0"
description = "Framework-neutral webhook, event, and exception capture for WatchWebhook"
readme = "README.md"
authors = [
    { name = "Pradeep Chauhan", email = "91078821+pradeep-lit@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = []


[build-system]
requires = ["uv_build>=0.11.9,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "watchwebhook"
